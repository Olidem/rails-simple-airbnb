<h1>My list of flats</h1>
<% @flats.each do |flat| %>
  <h2><%= link_to flat.name, flat_path(flat) %></h2>
  <p><%= flat.description %></p>
<% end %>

<h2><%= link_to "Add a flat", new_flat_path(@restaurant), class: "btn btn-primary" %></h2>
